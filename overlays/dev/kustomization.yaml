apiVersion: kustomize.config.k8s.io/v1beta1
kind: Kustomization

namespace: demo-dev
namePrefix: dev-

resources:
  - namespace.yaml
  - ../../base

images:
  - name: nginx
    newTag: "1.27"

patches:
  - path: patch-replicas.yaml
    target:
      kind: Deployment
      name: hello-kustomize
